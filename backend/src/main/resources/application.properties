server.port=${PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/pwdb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:pwdb}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:pwdb}
spring.datasource.hikari.maximum-pool-size=5

app.data.path=${APP_DATA_PATH:../virtualPatientData/pwdb/PWs/SQL/pressure_waveforms.db}
app.sample-rate-hz=${APP_SAMPLE_RATE_HZ:1000}
